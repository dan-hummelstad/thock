{
  "name": "thock",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @thock/keyboard build",
    "dev": "pnpm --filter @thock/keyboard dev",
    "test": "pnpm --filter @thock/keyboard test",
    "release": "pnpm build && wrangler deploy",
    "preview": "pnpm build && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}

# build/

electron-builder's `buildResources` directory (see `../electron-builder.yml`). Icons
(`icon.icns`, `icon.ico`, `icon.png`) and mac entitlements go here once they exist — not part of
this scaffold (P5 packaging/signing work).
